import {USER }from './generate'
import {STRING }from './verify'
import { EventList } from './EventList'


export default {
    GENERATE: {
        USER: USER
    },
    VERIFY: {
        STRING: STRING
    },
    EVENTS: EventList
}